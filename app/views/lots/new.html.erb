<h1>hello new</h1>

<div class="container">
<%= simple_form_for(@lot) do |f| %>
  <%= f.association :product %>
  <%= f.association :supplier %>
  <%= f.input :expiration_date %>
  <%= f.input :cost %>
  <%= f.input :quantity %>
  <%= f.button :submit %>
<% end %>
</div>

<% console %>
